import { WineColorPipe } from './wine-color.pipe';

describe('WineColorPipe', () => {
  it('create an instance', () => {
    const pipe = new WineColorPipe();
    expect(pipe).toBeTruthy();
  });
});
